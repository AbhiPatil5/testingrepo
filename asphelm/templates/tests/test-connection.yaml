apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asphelm.fullname" . }}-test-connection"
  labels:
    {{- include "asphelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asphelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
